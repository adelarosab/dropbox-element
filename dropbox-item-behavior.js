// Generated by CoffeeScript 1.10.0
(function() {
  var DropboxItemBehaviorImpl;

  DropboxItemBehaviorImpl = {
    properties: {

      /* If true, automatically performs fetch of your element */
      auto: Boolean,

      /* Path of your file inside your Dropbox Account */
      path: {
        reflectToAttribute: true,
        type: String
      },

      /* If you used share method you'll received into this attribute your sharing
      url
       */
      url: {
        notify: true,
        type: String
      }
    },

    /* Share your file among internet */
    share: function(options) {
      if (options == null) {
        options = {};
      }
      return this.instance.makeUrl(this.path, options, function(error, response) {
        if (error != null) {
          return this.fire("error", error);
        } else {
          return this.url = response.url;
        }
      });
    }
  };

  window.DropboxItemBehavior = [window.DropboxAuthBehavior, DropboxItemBehaviorImpl];

}).call(this);

//# sourceMappingURL=dropbox-item-behavior.js.map
